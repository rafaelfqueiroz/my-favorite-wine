import mongoose, { Schema } from "mongoose";
import { Wine } from "../../models";
import { WineRepository } from "../interfaces/wine.repository";

//const WineSchema: Schema = new Schema();
/* export const WineMongoDbRepository: WineRepository = mongoose.model<
    Wine,
    WineRepository
>("Wine", WineSchema); */
